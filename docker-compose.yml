version: '3.8'

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: anaoi-frontend
    ports:
      - "80:80"
    restart: unless-stopped
    networks:
      - anaoi-network

networks:
  anaoi-network:
    driver: bridge

